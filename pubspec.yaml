name: papilio
description: Build beautiful apps with Flutter. A declarative framework for state, composition and navigation.
version: 0.11.0-beta
homepage: https://github.com/MelbourneDeveloper/papilio
repository: https://github.com/MelbourneDeveloper/papilio
publish_to: none

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  ioc_container: ^0.6.0
  meta: ^1.7.0
  papilio_bloc:
    path: ../papilio_bloc

dev_dependencies:
  dart_code_metrics: ^4.17.1
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
