import 'package:flutter/material.dart';

///Simple implementation of [Route] purely to provide info about the
///current route
class PapilioRoute extends Route {
  PapilioRoute({RouteSettings? settings}) : super(settings: settings);
}
